# Inputs

Benchmark inputs are not stored in this repository unless they are owned, explicitly licensed, synthetic, public-domain, or otherwise permitted for public benchmark use.

Use `input_manifest.example.yaml` as the template for documenting local or public benchmark inputs. Each input should record provenance, permission basis, media type, intended benchmark stage, publication constraints, and any consent or likeness restrictions.
