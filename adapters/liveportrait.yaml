id: liveportrait
name: LivePortrait
repo_url: https://github.com/KwaiVGI/LivePortrait
env:
  python: CHECK_REQUIRED
  hardware: CHECK_REQUIRED
install:
  commands:
    - "echo '[dry-run] inspect LivePortrait installation instructions'"
weights:
  status: CHECK_REQUIRED
  commands:
    - "echo '[dry-run] verify LivePortrait weight access and license'"
smoke:
  commands:
    - "echo '[dry-run] plan LivePortrait official sample smoke test'"
benchmark_inputs:
  commands:
    - "echo '[dry-run] map standard source and driving inputs for LivePortrait'"
expected_outputs:
  - outputs/liveportrait/smoke.mp4
notes: Placeholder adapter; no downloads or inference are performed.
